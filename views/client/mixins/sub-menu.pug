// Định nghĩa một mixin tên là 'sub-menu' với 2 tham số: items (danh sách mục), path (đường dẫn gốc)
mixin sub-menu(items, path)
  // Tạo một danh sách không thứ tự (unordered list)
  ul
    // Lặp qua từng phần tử trong danh sách items
    each item in items
      li
        // Tạo một liên kết, href sẽ là path + slug của item
        // Hiển thị tiêu đề của mục (item.title)
        a(href=`${path}/${item.slug}`) #{item.title}
        
        // Nếu mục hiện tại có con (children) và số lượng con > 0
        if item.children && item.children.length > 0
          // Gọi đệ quy lại mixin sub-menu để hiển thị menu con
          +sub-menu(item.children, path)
