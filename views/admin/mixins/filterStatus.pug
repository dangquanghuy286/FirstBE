mixin filter-status(filterStatus)
   each item in filterStatus
            button(
              class=`btn btn-outline-success me-2 ${item.class}`,
              button-status=item.status) #{item.name}