mixin alert-success(time)
    - if (messages.success)
      .message.info
      div(
        class="alert alert-success"
        showAlert
        data-time=time
      ) #{messages.success}
        span(close-alert) X